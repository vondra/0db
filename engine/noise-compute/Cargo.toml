[package]
name = "noise-compute"
version = "0.1.0"
edition = "2021"
description = "Pure Rust noise computation engine — CNOSSOS-EU emission + ISO 9613-2 propagation"

# This is a LIBRARY crate — no I/O, no napi, no files.
# Used by source-reader (popup) and pipeline-worker (batch).
[lib]
name = "noise_compute"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
h3o = "0.7"
# SIMD for propagate_variants 8-band inner loop (stable, AVX2/AVX-512 with scalar fallback)
wide = "0.7"

[dev-dependencies]
# For unit tests
approx = "0.5"

[profile.release]
opt-level = 3
lto = "thin"
