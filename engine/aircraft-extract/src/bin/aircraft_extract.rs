//! `aircraft-extract` CLI — `stage-0` / `stage-1` / `stage-2a` /
//! `stage-2b` / `stage-2c` / `run-all` subcommands.
//!
//! Implemented in Step 14.

fn main() {
    eprintln!("aircraft-extract: skeleton — implemented in Step 14");
}
